version: '3.7'
services:
  prefect-agent:
    container_name: prefect-agent
    command: ["tail", "-f", "/dev/null"]
    build: .
    #volumes:
      #- ./app:/root/app
    #env_file:
      #- development.env
